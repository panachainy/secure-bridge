dev:
	poetry run uvicorn app.main:app --reload

# # Build and run with docker-compose
# docker-compose up --build

# # Or build manually
# docker build -t verification-service .
# docker run -p 8000:8000 verification-service
